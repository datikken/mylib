version: '3.1'

services:
  mongo:
    image: mongo
    restart: always
    ports:
      - 8000:8000
    volumes:
      - ./.env:/opt/app/.env
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: root